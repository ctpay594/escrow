# Admin portal

Admin Next.js app. Dev port **3002**. Run from the **monorepo root**.

```bash
pnpm dev:admin    # http://localhost:3002
```

Needs the backend on port 3000. No local `.env` required in development.

Mac setup: [`../../MAC.md`](../../MAC.md)
