# User portal (CTPay)

Merchant Next.js app. Dev port **3001**. Run from the **monorepo root**.

```bash
pnpm dev:user    # http://localhost:3001
```

Needs the backend on port 3000. No local `.env` required in development.

Mac setup: [`../../MAC.md`](../../MAC.md)
